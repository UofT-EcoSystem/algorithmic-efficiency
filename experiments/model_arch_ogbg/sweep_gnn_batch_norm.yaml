# WANDB_MODE=offline wandb sweep experiments/model_arch_ogbg/sweep_gnn_batch_norm.yaml
# wandb agent danielsnider/mlc_held_out_gnn/agolsekd
entity: danielsnider
project: mlc_held_out_gnn
name: sweep_gnn_batch_norm
description: ""
program: experiments/model_arch_ogbg/wandb_gnn_layer_norm.py
method: grid
metric:
  name: mean_average_precision
  goal: maximize
parameters:
  learning_rate:
    values: [0.0001, 0.001, 0.01]
  layer_norm:
    values: [True, False]
command:
  - python3
  - ${program}
  - ${args_json_file}

